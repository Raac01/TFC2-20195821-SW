import 'package:flutter/material.dart';

class AppColors {
  // Tonos principales
  static const Color primary = Color(0xFF4A90E2);      // Azul suave
  static const Color secondary = Color(0xFF7ED3B2);    // Verde agua
  static const Color accent = Color(0xFFFFC857);       // Amarillo cálido

  // Fondo y superficies
  static const Color background = Color(0xFFF5F7FA);   // Gris muy claro
  static const Color surface = Color(0xFFFFFFFF);      // Blanco

  // Texto
  static const Color textPrimary = Color(0xFF1A1A1A);
  static const Color textSecondary = Color(0xFF5A5A5A);

  // Estados
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
}
